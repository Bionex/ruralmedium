@layout('layouts/main')

@section('content')

  <div class="flex flex-col">
    <div>LISTANDO POSTS</div>
    @each(post in posts)
    <div class="flex flex-row">
      @form.button( {href: route('posts.show', {id: post.id}) })
        <div>post: {{ post.title }}</div>
        <div>by:{{post.user.email}}</div>
      @end
    </div>
    @end
  </div>
@end
