@layout('layouts/main')

@section('content')
  LISTANDO POSTS

  <div class="flex flex-col">
    @each(post in posts)
      @form.button( {href: route('posts.show', {id: post.id}) })
        post: {{ post.title }}
        by:{{post.user.email}}
      @end
    @end
  </div>
@end
