@layout('layouts/main')

@section('content')
  <div>
    <label>
      Email:
    </label>
    <span>
      {{ user.email }}
    </span>


    @if(user.coverId)
      <img src="{{ route('files.show', { id: user.coverId }) }}" />
    @end

  </div>
  @if(auth.user == user)
  <div>
    @form.button({ href: route('users.update')})
      Atualizar Perfil
    @end

  </div>
  @end
@end
